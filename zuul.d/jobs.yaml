- job:
    name: new-job
    parent: null
    nodeset:
      nodes:
        - label: ubuntu-jammy
          name: controller
    run: playbooks/test.yaml

- job:
    name: c-builder
    parent: base
    nodeset:
      nodes:
        - label: gcc-builder
          name: mynode
    run: playbooks/compile.yaml

